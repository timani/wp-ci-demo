# https://circleci.com/docs/configuration#machine
machine:
  php:
    # https://circleci.com/docs/environment#php
    version: 5.5.11 
  environment:
    # Set the site UUID here for now -> Private Environment variable
    PUUID: f86d7c5d-db0a-45f4-8a8a-dc0c37964bc1 
dependencies:
  pre:
    # Install
  # Install additional test dependencies here (like Casper, Behat, etc).
  override:   
    # Build the documentation 
    # - git remote add pantheon ssh://codeserver.dev.$PUUID@codeserver.dev.$PUUID.drush.in:2222/~/repository.git  
    #- git remote set-url pantheon ssh://codeserver.dev.$PUUID@codeserver.dev.$PUUID.drush.in:2222/~/repository.git
    #- git fetch pantheon/wp-meetup; 
    #- git checkout pantheon/wp-meetup # fetch branch from server and checkout locally 
    - git config --global user.email "timani@getpantheon.com"
    - git config --global user.name "Pantheon Timani"
    # - git push --force pantheon wp-meetup
test:
    #
    # This is where the documentation will be built. 
  override:
    # Build and deploy documentation
    - echo "WordPress SF Meetup" 
  # Perform post build operations  
  post:
# Custom notifications
# Deploy to Pantheon
# deployment:
#  live:
#    branch: master
#    commands:
#      - ./scripts/deploy.sh
notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
