machine:
  php:
    # https://circleci.com/docs/environment#php
    version: 5.5.11 
  environment:
    # Set the site UUID here for now -> Private Environment variable
    PUUID: f86d7c5d-db0a-45f4-8a8a-dc0c37964bc1 
dependencies:
  pre:
    # Install
  # Install additional test dependencies here (like Casper, Behat, etc).
  override:   
    # Build the documentation  
    - git remote add pantheon ssh://codeserver.dev.f86d7c5d-db0a-45f4-8a8a-dc0c37964bc1@codeserver.dev.f86d7c5d-db0a-45f4-8a8a-dc0c37964bc1.drush.in:2222/~/repository.git 
    - git config --global push.default simple
    - git config --global user.email "timani27@gmail.com"
    - git config --global user.name "Pantheon Timani"
    - git pull pantheon wp-meetup
    - git push pantheon wp-meetup 
    # - git log # fetch branch from server and checkout locally 
    - git remote -v
